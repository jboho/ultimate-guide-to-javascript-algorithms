/*
  Return the first element from the given array that passes
  the test specified by the provided function.

  Both the array and function are passed into the function
  as arguments as shown below:
*/

function arrayFilter(arr, func) {
  // Code goes here
}

module.exports = arrayFilter;
