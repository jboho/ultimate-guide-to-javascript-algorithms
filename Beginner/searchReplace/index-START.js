/*
  Given a string of text, write an functiion that replaces a word in the string
  and has the same casing as the replaced word.
  e.g. searchReplace('He is Sleeping on the couch', 'Sleeping', 'sitting')
  // should return ''He is Sitting on the couch'
*/

function searchReplace(str, word, newWord) {
  // Code goes here
}

module.exports = searchReplace;
